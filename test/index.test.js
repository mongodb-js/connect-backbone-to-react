var connectBackboneToReact = require('../');
var assert = require('assert');

describe('connect-backbone-to-react', function() {
  it('should work', function() {
    assert(connectBackboneToReact);
  });
});
